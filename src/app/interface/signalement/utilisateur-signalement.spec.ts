import { UtilisateurSignalement } from './utilisateur-signalement';

describe('UtilisateurSignalement', () => {
  it('should create an instance', () => {
    expect(new UtilisateurSignalement()).toBeTruthy();
  });
});
